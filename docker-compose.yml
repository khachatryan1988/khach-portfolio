services:
  web:
    build: .
    container_name: portfolio_web
    env_file: .env
    ports:
      - "${APP_PORT}:8000"
    volumes:
      - ./:/app
    working_dir: /app
    command: >
      bash -lc "
      uvicorn app.main:app --host 0.0.0.0 --port 8000
      "
